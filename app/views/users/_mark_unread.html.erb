<%= form_tag({controller: "markings", action: "create" }, remote: true) do |f| %>

  <div><%= hidden_field_tag("micropost_id", id) %></div>
  <%= submit_tag("Mark as Unread", class: "btn btn-marking") %>

<% end %>